---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vikunja-config
  namespace: collaboration
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 50Mi
