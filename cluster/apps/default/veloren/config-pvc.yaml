---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: veloren-config-v1
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 20Gi
