---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariadb2-v1
  namespace: database
  labels:
    app.kubernetes.io/name: &name mariadb2
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 10Gi
