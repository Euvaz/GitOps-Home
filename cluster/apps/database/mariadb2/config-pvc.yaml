---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariadb2-v1
  namespace: database
  labels:
    app.kubernetes.io/name: mariadb
    app.kubernetes.io/instance: mariadb2
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 10Gi
