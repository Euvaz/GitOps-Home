---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - config-pvc.yaml
  - helm-release.yaml
  - secret.sops.yaml
configMapGenerator:
  - name: obsidian-couchdb-config-v1
    files:
      - config/config.ini
generatorOptions:
  disableNameSuffixHash: true
  annotations:
    kustomize.toolkit.fuxcd.io/substitute: disabled
