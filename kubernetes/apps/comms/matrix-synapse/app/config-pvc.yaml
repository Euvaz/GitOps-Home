---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matrix-redis-v1
  namespace: comms
#  labels:
#    app.kubernetes.io/name: &name matrix-synapse
#    app.kubernetes.io/instance: *name
#    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 1Gi
